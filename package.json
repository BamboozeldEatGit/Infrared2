{
  "name": "proxy-switcher",
  "version": "1.0.0",
  "description": "A proxy switcher for Doge Unblocker and Interstellar",
  "main": "server.js",
  "scripts": {
    "setup": "node setup.js",
    "start": "node server.js",
    "deploy": "node setup.js && node server.js",
    "vercel-build": "npm install && npm run setup"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "simple-git": "^3.22.0",
    "dotenv": "^16.0.3",
    "cors": "^2.8.5",
    "fs-extra": "^11.1.0"
  }
} 